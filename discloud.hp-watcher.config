NAME=SniperPlug-HP-Watcher
TYPE=bot
MAIN=hp_watcher_main.py
RAM=256
# Keep the shared libSQL/Turso driver on the same stable interpreter as SniperPlug.
VERSION=3.11
AUTORESTART=true
BUILD=pip install -r requirements.txt
