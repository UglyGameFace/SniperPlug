# SniperPlug Website

This directory contains the standalone SniperPlug website served at `https://sniperplug.com`.

It is intentionally isolated from the Discord bot runtime. Website dependencies, builds, functions, content, and deployment configuration must remain inside this directory unless a repository-level file is strictly required for CI or host routing.
